version: 2
updates:
  # Keep the SHA-pinned (and floating) GitHub Actions current so security
  # updates land without manual bumps.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci

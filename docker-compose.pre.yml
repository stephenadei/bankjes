services:
  bankjes:
    image: ghcr.io/stephenadei/bankjes:pre
    container_name: bankjes-pre
    pull_policy: always
    restart: unless-stopped
    env_file:
      - .env.pre
    volumes:
      - ./data:/data
    ports:
      - "127.0.0.1:4310:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/healthz')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
